# SQL

Use this folder for optional SQL analysis.

Good candidates:

- Load the raw CSV into SQLite or DuckDB.
- Create a clean customer experiment table.
- Write group summary queries.
- Write segment-level lift queries.

SQL is optional for the project, but including a few clean queries can strengthen the portfolio because it shows you can analyze experiments outside Python.
